# RAIBITSERVER production deploy

Production must use managed PostgreSQL for the control-plane DB, isolated Kubernetes runtime clusters, signed images, vulnerability scanning, audit logs, and quota enforcement.
