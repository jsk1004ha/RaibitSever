apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: managedcaches.raibitserver.io
spec:
  group: raibitserver.io
  names:
    kind: ManagedCache
    plural: managedcaches
  scope: Namespaced
  versions:
    - name: v1alpha1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          x-kubernetes-preserve-unknown-fields: true
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: managedobjectstorages.raibitserver.io
spec:
  group: raibitserver.io
  names:
    kind: ManagedObjectStorage
    plural: managedobjectstorages
  scope: Namespaced
  versions:
    - name: v1alpha1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          x-kubernetes-preserve-unknown-fields: true
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: managedvectordatabases.raibitserver.io
spec:
  group: raibitserver.io
  names:
    kind: ManagedVectorDatabase
    plural: managedvectordatabases
  scope: Namespaced
  versions:
    - name: v1alpha1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          x-kubernetes-preserve-unknown-fields: true
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: managedmessagequeues.raibitserver.io
spec:
  group: raibitserver.io
  names:
    kind: ManagedMessageQueue
    plural: managedmessagequeues
  scope: Namespaced
  versions:
    - name: v1alpha1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          x-kubernetes-preserve-unknown-fields: true
