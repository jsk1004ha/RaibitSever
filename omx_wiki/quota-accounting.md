# Quota accounting

Runtime quota usage is calculated from memberships -> projects -> services/resources/deployments plus usage records. It includes build minutes, runtime hours, CPU millicores, memory MB, storage, daily deployments, and preview deployments.
